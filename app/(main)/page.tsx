import * as React from "react";
import { MainModule } from "./_modules";
import { NextPage } from "next";

const Page: NextPage = (): React.ReactElement => {
  return <MainModule />;
};
export default Page;
